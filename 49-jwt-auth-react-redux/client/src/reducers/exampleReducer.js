const defaultState = {
  non: "sense",
  key: "values"
}

const exampleReducer = /*FUNCTION*/ (state=defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default exampleReducer
